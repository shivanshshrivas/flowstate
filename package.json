{
  "name": "flowstate-monorepo",
  "private": true,
  "workspaces": [
    "gateway",
    "demo-store"
  ],
  "scripts": {
    "gateway:build": "npm run build --workspace=gateway",
    "gateway:dev": "npm run dev --workspace=gateway",
    "demo:dev": "npm run dev --workspace=demo-store",
    "demo:build": "npm run build --workspace=demo-store"
  }
}
