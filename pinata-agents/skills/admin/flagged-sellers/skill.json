{
  "name": "flagged-sellers",
  "description": "Get sellers flagged for high dispute rates or policy violations",
  "parameters": {
    "type": "object",
    "properties": {
      "project_id": {
        "type": "string",
        "description": "The platform project ID (from SYSTEM_CONTEXT user_id)"
      },
      "threshold": {
        "type": "number",
        "description": "Minimum dispute rate to flag (0.0 to 1.0, default 0.1 = 10%)"
      }
    },
    "required": ["project_id"]
  }
}
