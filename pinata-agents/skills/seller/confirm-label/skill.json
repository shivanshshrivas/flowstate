{
  "name": "confirm-label",
  "description": "Confirm that the seller has printed the shipping label for an order, triggering the first payout milestone",
  "parameters": {
    "type": "object",
    "properties": {
      "seller_id": {
        "type": "string",
        "description": "The seller's wallet address or ID (from SYSTEM_CONTEXT user_id)"
      },
      "order_id": {
        "type": "string",
        "description": "The order ID for which the label has been printed"
      }
    },
    "required": ["seller_id", "order_id"]
  }
}
